import { Network } from "../types";
export declare const createNetworkInterface: (chainId: number, customNetworks: Network[]) => Network;
export declare const extractEndpoints: (chainId: number, customNetworks: Network[]) => string[];
export declare const defaultNetworks: {
    name: string;
    chainId: number;
    blockHeight: number;
    publicEndpoints: never[];
    explorerUrl: string;
}[];
export declare const knownNetworks: {
    name: string;
    chainId: number;
    blockHeight: number;
    publicEndpoints: string[];
    explorerUrl: string;
}[];
