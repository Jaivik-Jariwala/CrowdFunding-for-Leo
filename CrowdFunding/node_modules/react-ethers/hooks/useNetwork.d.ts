import { Network, Provider } from "../types";
export declare const useNetwork: (setProvider: React.Dispatch<React.SetStateAction<Provider>>, setNetwork: React.Dispatch<React.SetStateAction<Network>>, provider: Provider, autoRefresh: boolean, chainId: number, customNetworks: Network[]) => void;
