/**
 * Should detect the wallet
 * @returns Web3Provider
 */
import { ConnectionType } from "../types";
import { Account, Provider } from "../types";
export declare const useInjection: (connectionType: ConnectionType, setProvider: React.Dispatch<React.SetStateAction<Provider>>, setAccount: React.Dispatch<React.SetStateAction<Account>>, provider: Provider) => void;
