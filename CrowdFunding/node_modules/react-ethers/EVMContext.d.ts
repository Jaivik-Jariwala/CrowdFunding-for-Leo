import React, { ReactNode } from "react";
import { ApiKeysOption, ConnectionType, ContextLoad, Network } from "./types";
export declare type Props = {
    children: ReactNode;
    defaultConnectionType: ConnectionType;
    autoRefresh: boolean;
    customNetworks: Network[];
    chainId: number;
    apiKeys: ApiKeysOption;
};
export declare const Context: React.Context<ContextLoad>;
export declare const EVMContext: ({ children, defaultConnectionType, autoRefresh, customNetworks, chainId, apiKeys, }: Props) => JSX.Element;
