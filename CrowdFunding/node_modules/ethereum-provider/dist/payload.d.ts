import type { Payload } from './types';
export declare type JsonRpcPayload = Payload & {
    readonly params: readonly unknown[];
    readonly jsonrpc: '2.0';
    readonly id: number;
    chainId?: string;
};
export declare function create(method: string, params: readonly unknown[] | undefined, id: number, targetChain?: string): JsonRpcPayload;
